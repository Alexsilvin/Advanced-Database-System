

# Run and deploy your AI Studio app
app was deployed using Ai studio for fast access

This contains everything you need to run your app locally.

##changes
changes where made to look like a template, so the other members should start coding from it
```
-basic database done, setup the rest
-basic ui, landing page done!
-connection between
what i have achieved;
- Successfully connected pgAdmin(postgres user friendly editor) with neon console( an online  data hosting service)
- implemented the changes in protocols
- implemented the database
- Generated mock data for teacher review
- hard coded the basic ui screen ensuring retro-gaming perspectivesnow what needs to be done;
- you guys can now freely edit the pages// make sure to keep consistency and clean app flow
- we will discuss further about the data on campus
- The website will be deployed in a few minute for teacher's review


## Run Locally
set up the .env file

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
